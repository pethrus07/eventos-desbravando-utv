# Cloudflare / Wrangler (cache e artefatos locais)
.wrangler/
node_modules/
dist/

# Segredos / variaveis de ambiente
.env
.env.*
.dev.vars

# DADOS PESSOAIS REAIS (CPF / financeiro / CRM) — NUNCA versionar nem enviar pra nuvem.
# O arquivo continua no disco como backup local; so nao entra no git.
seed_dados_reais.sql

# Backups do banco de producao (contem dados reais) — nunca versionar
backup*.sql
*.backup.sql

# Sistema
.DS_Store
Thumbs.db
